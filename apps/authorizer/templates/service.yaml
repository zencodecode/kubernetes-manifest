apiVersion: v1
kind: Service
metadata:
  name: {{ include "authorizer-service.fullname" . }}
  labels:
    {{- include "authorizer-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  sessionAffinity: None
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "authorizer-service.selectorLabels" . | nindent 4 }}

